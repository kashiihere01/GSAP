

    // gsap.from means Final to Intial
    gsap.from("h1",{
    opacity:0,
        duration:1,
        y:30,
        delay:1,
        // work like loop will show data one by one
        stagger:0.5      
        })